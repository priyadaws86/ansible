- name: Install Packages
  hosts: local
  connection: local
  become: yes

  tasks:
  - name: Print names
    ansible.builtin.package:
      name: " {{ item }}"
      state: latest
    
    loop:
    - nginx
    - mysql
    - zip